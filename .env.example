# =========================================================
# 环境变量配置示例
# 复制本文件为 .env 并按实际路径修改
# =========================================================

# --- Ollama 服务 ---
# Ollama 默认运行在 http://localhost:11434，通常无需修改
# 若 Ollama 部署在远程服务器，取消注释并修改：
# OLLAMA_BASE_URL=http://localhost:11434

# --- 嵌入模型（可选，默认 bge-m3）---
# 需先在 Ollama 中拉取模型：ollama pull bge-m3
EMBEDDING_MODEL=bge-m3

# --- OCR 说明 ---
# 图片/PDF 文字识别由 RapidOCR 提供（pip 依赖自带 ONNX 模型），无需安装 Tesseract。

# --- HuggingFace 镜像（国内加速，可选）---
# 自动使用 hf-mirror.com 镜像下载 Rerank 模型，无需手动设置
# HF_ENDPOINT=https://hf-mirror.com

# --- LangSmith 评测/追踪（可选，评测页「全链路评测」可一键上报实验看板）---
# 1) 注册并创建 API Key：https://smith.langchain.com → Settings → API Keys
# 2) 配置 LANGSMITH_API_KEY 后，评测页结果区出现「☁️ 上报为 LangSmith 实验」按钮
# 注意：上报内容包含问题/回答/命中文档摘要，会上传至 LangSmith SaaS（你的账号空间）
# LANGSMITH_API_KEY=lsv2_pt_xxxxxxxxxxxxxxxx
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# 置为 true 后，线上对话的链/图调用也会自动 trace 到 LangSmith（默认关闭）
# LANGSMITH_TRACING=false
# LangSmith 项目名（实验与 trace 归属的项目）
# LANGSMITH_PROJECT=rag-customer-service

# --- 云端模型 API Key（可选，在「模型设置」页切换 API 模式时使用）---
# 全部走 OpenAI 兼容协议；在主页侧边栏选择厂商后，此处对应的 Key 自动生效。
# 也可不写 .env，直接在页面临时输入 Key（仅本次运行有效，不落盘）。
# DeepSeek（仅聊天模型，无嵌入 API）：https://platform.deepseek.com
# DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
# 阿里云百炼 DashScope（聊天 qwen 系列 + 嵌入 text-embedding-v4）：https://bailian.console.aliyun.com
# DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxx
# SiliconFlow 硅基流动（聊天 + 嵌入 BAAI/bge-m3，与本地 bge-m3 同向量空间）：https://siliconflow.cn
# SILICONFLOW_API_KEY=sk-xxxxxxxxxxxxxxxx
# 自定义 OpenAI 兼容端点的 Key（厂商选「自定义」时读取）
# OPENAI_COMPAT_API_KEY=sk-xxxxxxxxxxxxxxxx

# --- 访问安全（发布到公网时建议配置）---
# REST API（api.py）的访问令牌：调用方需在请求头 X-API-Key 携带此令牌，否则接口拒绝访问
# 请设置为足够随机的字符串（如 python -c "import secrets; print(secrets.token_urlsafe(32))"）
# API_ACCESS_TOKEN=请设置一个随机令牌

