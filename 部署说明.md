# 部署说明

本项目是「智能客服 Agent」系统，可打包成 Docker 部署到其他电脑。有两种方式，按目标电脑是否有网络选择。

---

## 方式一：在线构建部署（目标电脑有 Docker + 网络）

适合：目标电脑能联网、在公司内网。

### 步骤

1. **拷贝项目**：把整个项目目录拷到目标电脑（代码 + `config.yaml` + `.env` + `chroma_db/` + `data/` + `docs/`）。
2. **安装 Docker**：[Docker Desktop](https://www.docker.com/products/docker-desktop/)（Win/Mac）或 Docker Engine + Compose（Linux）。
3. **启动**：在项目目录运行：
   ```bash
   docker compose up -d --build
   ```
   首次会自动：构建镜像 → 启动 Ollama → 拉取 `qwen2.5:1.5b` 聊天 + `bge-m3` 嵌入模型（约 2GB）。
4. **访问**：http://localhost:8501

查看模型拉取进度：`docker logs -f ollama-init`

---

## 方式二：离线部署包（目标电脑无网络）

适合：内网隔离机、离线演示环境等无法联网的环境。

### 第 1 步：在有网络的电脑上导出部署包

```bash
bash scripts/docker_export.sh
```

脚本会自动：构建应用镜像 → 拉取 Ollama 镜像和本地模型（`qwen2.5:1.5b` + `bge-m3`）→ 导出镜像 tar → 归集知识库/配置/文档，最终生成 `docker_deploy/` 目录（约 3~4GB）。

### 第 2 步：拷贝到目标电脑

把整个 `docker_deploy/` 目录拷过去（U 盘需 **exFAT/NTFS** 格式，FAT32 装不下大文件）。

### 第 3 步：目标电脑启动

目标电脑只需装 Docker，然后进入 `docker_deploy/` 运行：

- Linux/macOS：`./start.sh`
- Windows：双击或用 Git Bash 运行 `./start.sh`

浏览器访问：http://localhost:8501

---

## 启动 / 停止常用命令

```bash
docker compose ps              # 查看服务状态
docker logs -f smart-cs-bot   # 查看应用日志
docker compose down            # 停止服务
```

---

## ⚠️ 注意事项

1. **聊天模型**：默认走本地 `qwen2.5:1.5b`（随 Ollama 启动，无需网络）；若在「模型设置」切换为云端/内网 API（Base URL），则目标电脑必须能访问对应网络，否则聊天连不上（知识库检索、本地嵌入不受影响）。
2. **嵌入模型 `bge-m3`**：由 compose 内 Ollama 服务提供，无需单独安装。
3. **Rerank 模型**：若离线部署包未包含 `models/`（rerank 权重），目标电脑首次开启 Rerank 时需联网下载（约 1GB）；不开启 Rerank 则无影响。
4. **API Key**：`.env` 内配置的 API Key / `API_ACCESS_TOKEN` 会随部署包一起拷贝，仅限受控环境使用，注意保管、勿外发。
